import React from "react";
import PropTypes from "prop-types";

import Title from "./Title";

export default {
  title: "Title",
  component: Title,
  tags: ["autodocs"],
};

export const Template = {
  args: {
    title: "Title",
  },
};
